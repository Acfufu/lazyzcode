# f1-dual-wave（v024-fast-exp#N4）

双波分工夹具：波甲=average（N1+N2），波乙=median（N3+N4），跨波零文件交集。
F1=worker-local 本波断言（两工人各自取证 ⇒ 后取证 supersede 先取证 ⇒ dag supersedes 边 ≥1 可观测）。
合并态断言在 verdict/run.sh（merge 后的 scratch 根）。
