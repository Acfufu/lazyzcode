# Task brief — string case converters

Read `SPEC.md` and implement the four functions in `lib/cases.js`:
`toCamel`, `toSnake`, `toKebab`, `toConstant`. Each currently throws
"not implemented".

`node check.mjs` must print `PASS` when all four are done.

This is a larger piece of work. If you cannot finish in one sitting, leave the
state of your work clear so whoever continues can pick it up without redoing
what is already done.
