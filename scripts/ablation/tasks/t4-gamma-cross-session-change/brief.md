# Task — string utilities phase 1: pad

Read `SPEC.md`. Implement `pad` in `lib/pad.js` (it currently throws
"not implemented"). `lib/slug.js` is already implemented and its current
behavior must not change in this phase — do not modify it.

Run your normal goal-loop workflow for this work: register a goal, plan,
execute, and capture the required evidence as you go. `node check.mjs` must
print `PASS` (slug and pad sections both green).

This session has a phase 2: a second change batch will arrive in this same
session right after this phase. Therefore, when phase 1 is done and verified,
STOP — leave the goal in its current active state; do NOT finish it now.
